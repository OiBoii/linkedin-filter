{
  "name": "inrole-extension",
  "version": "1.1.0",
  "private": true,
  "description": "inRole Chrome extension release tooling",
  "scripts": {
    "lint": "eslint extension/src --ext .js",
    "format": "prettier --write \"extension/**/*.{js,html,css,json}\" \"docs/**/*.md\"",
    "format:check": "prettier --check \"extension/**/*.{js,html,css,json}\" \"docs/**/*.md\"",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "build": "rm -rf dist && mkdir -p dist/inrole && cp -R extension/* dist/inrole/",
    "package": "npm run build && cd dist && rm -f inrole-extension.zip && zip -r inrole-extension.zip inrole",
    "ci": "npm run lint && npm run format:check && npm run test:unit && npm run test:e2e && npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "eslint": "^9.34.0",
    "globals": "^15.9.0",
    "prettier": "^3.3.3",
    "vitest": "^2.0.5"
  }
}
